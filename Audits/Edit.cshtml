@page
@model Test3.Pages.Audits.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Audit</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Audit.AID" />
            <div class="form-group">
                <label asp-for="Audit.Save" class="control-label"></label>
                <input asp-for="Audit.Save" class="form-control" />
                <span asp-validation-for="Audit.Save" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Audit.Edit" class="control-label"></label>
                <input asp-for="Audit.Edit" class="form-control" />
                <span asp-validation-for="Audit.Edit" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
